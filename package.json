{
  "name": "household-hub-v2",
  "private": true,
  "description": "Netlify Functions dependencies for the household hub app (no frontend build step — index.html stays plain static/vanilla JS).",
  "dependencies": {
    "@netlify/functions": "^6.0.0",
    "firebase-admin": "^14.4.0",
    "web-push": "^3.6.7"
  }
}
